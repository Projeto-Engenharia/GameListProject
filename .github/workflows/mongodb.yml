name: Mongo Export

on: 
  push:
  pull_request:

jobs:
  mongoexport:
    runs-on: ubuntu-latest
    name: Export MongoDB Json file
    steps:
      - uses: actions/checkout@v2
      - name: Export Json
        uses: tezz-io/mongoexport@v1
        with:
          uri: ${{ mongodb+srv://chuckbabyReal:rudneygay123@gamelist.fkndq.mongodb.net/test }}
          export: D:\Dev\GameListProject_real\GameListProject\result.json
          db: GameList
          collection: Users
          rewrite: true
